/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'],  // Asegúrate de permitir estos dominios
      },
};

export default nextConfig;
